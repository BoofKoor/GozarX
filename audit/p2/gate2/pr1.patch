diff --git a/frontend/site/app/globals.css b/frontend/site/app/globals.css
index 78ebeee..114751e 100644
--- a/frontend/site/app/globals.css
+++ b/frontend/site/app/globals.css
@@ -109,17 +109,17 @@ a{ color:inherit; }
    under RTL. One global rule covers every context; the `.cta:hover` rule below adds the slide. */
 [dir="rtl"] .ic-dir{ transform:scaleX(-1); }
 .btn.secondary{ background:var(--surface); color:var(--text); border-color:var(--border-strong); }
-.btn.secondary:hover{ border-color:var(--primary); color:var(--link); background:var(--brand-tint); }
+@media (hover:hover){ .btn.secondary:hover{ border-color:var(--primary); color:var(--link); background:var(--brand-tint); } }
 .btn.ghost{ background:transparent; color:var(--text); font-weight:600; }
-.btn.ghost:hover{ background:var(--brand-tint); color:var(--link); }
+@media (hover:hover){ .btn.ghost:hover{ background:var(--brand-tint); color:var(--link); } }
 .btn.lg{ font-size:16px; padding:14px 24px; }
 .cta{ inline-size:100%; font-size:16.5px; font-weight:800; padding:16px 22px; border-radius:16px; color:#fff; background:linear-gradient(135deg,var(--hero-1),color-mix(in srgb,var(--hero-1) 55%,var(--hero-2))); box-shadow:var(--glow); position:relative; overflow:hidden; }
 [dir="rtl"] .cta{ background:linear-gradient(225deg,var(--hero-1),color-mix(in srgb,var(--hero-1) 55%,var(--hero-2))); }
 #app[data-theme="dark"] .cta{ color:#EAF2FF; }
-.cta:hover{ filter:brightness(1.06); box-shadow:var(--glow),0 0 0 4px color-mix(in srgb,var(--primary) 16%,transparent); }
+@media (hover:hover){ .cta:hover{ filter:brightness(1.06); box-shadow:var(--glow),0 0 0 4px color-mix(in srgb,var(--primary) 16%,transparent); } }
 .cta .ic-dir{ transition:transform .2s; }
-.cta:hover .ic-dir{ transform:translateX(3px); }
-[dir="rtl"] .cta:hover .ic-dir{ transform:translateX(-3px) scaleX(-1); }
+@media (hover:hover){ .cta:hover .ic-dir{ transform:translateX(3px); } }
+@media (hover:hover){ [dir="rtl"] .cta:hover .ic-dir{ transform:translateX(-3px) scaleX(-1); } }
 .spinner{ inline-size:19px; block-size:19px; border-radius:50%; border:2.5px solid currentColor; border-inline-start-color:transparent; animation:spin .7s linear infinite; }
 @keyframes spin{ to{ transform:rotate(360deg); } }
 .eyebrow{ display:inline-flex; align-items:center; gap:7px; font-size:12px; font-weight:700; color:var(--brand-tint-ink); background:var(--brand-tint); border:1px solid var(--brand-tint-2); border-radius:var(--r-pill); padding:5px 13px; }
@@ -130,14 +130,14 @@ a{ color:inherit; }
 .lang-banner a{ font-weight:700; text-decoration:underline; cursor:pointer; }
 .lang-banner .x{ border:0; background:transparent; color:inherit; cursor:pointer; font-size:16px; line-height:1; inline-size:32px; block-size:32px; display:inline-grid; place-items:center; border-radius:8px; flex:none; }
 .lang-banner.hide{ display:none; }
-header.hd{ position:sticky; inset-block-start:0; z-index:60; background:color-mix(in srgb,var(--bg) 82%,transparent); backdrop-filter:blur(18px) saturate(1.4); border-block-end:1px solid var(--border); }
+header.hd{ position:sticky; inset-block-start:0; z-index:60; background:color-mix(in srgb,var(--bg) 94%,transparent); backdrop-filter:blur(18px) saturate(1.4); border-block-end:1px solid var(--border); }
 .hd-row{ display:flex; align-items:center; gap:16px; padding-block:12px; }
-@media (max-width:360px){ .hd-row{ gap:6px; } .hd-ctrls{ gap:6px; } .icon-only{ inline-size:36px; block-size:36px; } }
+@media (max-width:360px){ .hd-row{ gap:6px; } .hd-ctrls{ gap:6px; } }
 .brandmark{ display:flex; align-items:center; gap:10px; font-weight:800; font-size:17px; text-decoration:none; color:var(--text); }
 nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px; }
 @media (min-width:1000px){ nav.mainnav{ display:flex; } }
 .navlink{ font-size:14.5px; font-weight:600; color:var(--muted); text-decoration:none; padding:8px 12px; border-radius:10px; transition:color .15s,background .15s; }
-.navlink:hover{ color:var(--text); background:var(--sunken); }
+@media (hover:hover){ .navlink:hover{ color:var(--text); background:var(--sunken); } }
 .navlink.active{ color:var(--link); background:var(--brand-tint); }
 .hd-spacer{ flex:1; }
 .hd-ctrls{ display:flex; align-items:center; gap:8px; }
@@ -145,8 +145,8 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 .seg button{ border:0; background:transparent; color:var(--muted); font:inherit; font-size:13px; font-weight:600; padding:5px 12px; border-radius:var(--r-pill); cursor:pointer; }
 .seg button[aria-pressed="true"]{ background:var(--primary); color:var(--on-primary); font-weight:700; }
 .seg button:focus-visible{ outline:2px solid var(--ring); outline-offset:2px; }
-.icon-only{ inline-size:38px; block-size:38px; border-radius:11px; border:1px solid var(--border); background:var(--surface); color:var(--text); display:grid; place-items:center; cursor:pointer; }
-.icon-only:hover{ border-color:var(--primary); color:var(--link); }
+.icon-only{ inline-size:44px; block-size:44px; border-radius:13px; border:1px solid var(--border); background:var(--surface); color:var(--text); display:grid; place-items:center; cursor:pointer; }
+@media (hover:hover){ .icon-only:hover{ border-color:var(--primary); color:var(--link); } }
 .icon-only .ic{ inline-size:19px; block-size:19px; }
 .status-btn{ display:none; }
 .burger{ display:grid; }
@@ -195,13 +195,13 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 .theme-switch.is-dark .slot.moon{ color:var(--on-primary); }
 /* ============ sections ============ */
   section{ position:relative; }
-.sec{ padding-block:56px; }
-.sec-head{ text-align:center; max-width:44rem; margin:0 auto 40px; }
+.sec{ padding-block:28px; }
+.sec-head{ text-align:center; max-width:44rem; margin:0 auto 24px; }
 .sec-head .eyebrow{ margin-block-end:14px; }
 .sec-title{ font-size:clamp(26px,4.6vw,38px); font-weight:800; line-height:var(--lh-head); letter-spacing:var(--ls-head); margin:0 0 12px; text-wrap:balance; }
-.sec-sub{ font-size:16px; color:var(--muted); margin:0; }
+.sec-sub{ font-size:16px; color:var(--muted); margin:0; text-wrap:pretty; }
 .link-more{ display:inline-flex; align-items:center; gap:6px; font-weight:700; color:var(--link); text-decoration:none; font-size:14.5px; }
-.link-more:hover{ text-decoration:underline; }
+@media (hover:hover){ .link-more:hover{ text-decoration:underline; } }
 .link-more .ic{ inline-size:16px; block-size:16px; }
 /* HERO */
   .hero{ position:relative; overflow:hidden; padding-block:32px 52px; }
@@ -260,7 +260,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 .loc-grid2{ display:grid; grid-template-columns:repeat(3,1fr); gap:8px; }
 @media (min-width:520px){ .loc-grid2{ grid-template-columns:repeat(4,1fr); } }
 .loc-card{ position:relative; display:flex; flex-direction:column; align-items:center; gap:9px; padding:13px 6px 11px; border:1.5px solid var(--border); border-radius:var(--r-card); background:var(--surface); cursor:pointer; transition:border-color .16s,background .16s,transform .12s,box-shadow .16s; min-inline-size:0; }
-.loc-card:hover{ border-color:color-mix(in srgb,var(--primary) 45%,var(--border-strong)); transform:translateY(-2px); box-shadow:var(--shadow-sm); }
+@media (hover:hover){ .loc-card:hover{ border-color:color-mix(in srgb,var(--primary) 45%,var(--border-strong)); transform:translateY(-2px); box-shadow:var(--shadow-sm); } }
 .loc-card:focus-visible{ outline:2px solid var(--ring); outline-offset:2px; }
 .loc-card[aria-pressed="true"]{ border-color:var(--primary); background:var(--brand-tint); box-shadow:0 0 0 3px color-mix(in srgb,var(--primary) 14%,transparent); }
 .loc-card .flag-wrap{ position:relative; }
@@ -296,7 +296,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 .reassure{ display:flex; flex-wrap:wrap; align-items:center; justify-content:center; gap:6px 14px; margin-block-start:14px; }
 .reassure .r{ display:inline-flex; align-items:center; gap:5px; font-size:12.5px; color:var(--muted); }
 .reassure .r .ic{ inline-size:14px; block-size:14px; color:var(--success); }
-.antibot{ display:flex; align-items:center; justify-content:center; gap:6px; margin-block-start:12px; font-size:11.5px; color:var(--faint); }
+.antibot{ display:flex; align-items:center; justify-content:center; gap:6px; margin-block-start:12px; font-size:11.5px; color:var(--muted); }
 .antibot .ic{ inline-size:13px; block-size:13px; }
 .status-head{ display:flex; align-items:center; gap:13px; margin-block-end:18px; }
 .status-ic{ inline-size:46px; block-size:46px; border-radius:14px; display:grid; place-items:center; flex:none; }
@@ -335,13 +335,13 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 /* list-row style: icon → name → trailing chevron (the whole row is the deep link) */
 .app-btn{ display:inline-flex; align-items:center; gap:9px; flex:1; justify-content:flex-start; direction:ltr; font:inherit; font-size:12.5px; font-weight:600; color:var(--text); text-decoration:none; background:var(--surface); border:1px solid var(--border); border-radius:var(--r-ctl); padding:10px 12px; cursor:pointer; transition:border-color .15s; }
 .app-btn .app-chev{ margin-inline-start:auto; inline-size:15px; block-size:15px; color:var(--muted); opacity:.75; flex:none; transition:color .15s, opacity .15s, transform .15s; }
-.app-btn:hover{ border-color:var(--primary); }
-.app-btn:hover .app-chev{ color:var(--primary); opacity:1; transform:translateX(2px); }
+@media (hover:hover){ .app-btn:hover{ border-color:var(--primary); } }
+@media (hover:hover){ .app-btn:hover .app-chev{ color:var(--primary); opacity:1; transform:translateX(2px); } }
 .app-btn:focus-visible{ outline:2px solid var(--ring); outline-offset:2px; }
 .app-ico{ inline-size:26px; block-size:26px; border-radius:7px; flex:none; object-fit:cover; box-shadow:inset 0 0 0 1px var(--flag-ring),0 1px 2px rgba(2,6,23,.18); }
 .cfg-actions{ display:flex; gap:8px; margin-block-start:12px; }
 .icon-btn{ display:inline-flex; align-items:center; justify-content:center; gap:7px; flex:1; font:inherit; font-size:13px; font-weight:600; color:var(--text); background:var(--surface); border:1px solid var(--border-strong); border-radius:var(--r-ctl); padding:10px 12px; cursor:pointer; }
-.icon-btn:hover{ border-color:var(--primary); color:var(--link); background:var(--brand-tint); }
+@media (hover:hover){ .icon-btn:hover{ border-color:var(--primary); color:var(--link); background:var(--brand-tint); } }
 .icon-btn .ic{ inline-size:16px; block-size:16px; }
 .qr-panel{ margin-block-start:12px; display:none; justify-content:center; }
 .qr-panel.show{ display:flex; }
@@ -367,11 +367,11 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 .missions-head .t{ font-size:13.5px; font-weight:700; display:inline-flex; align-items:center; gap:7px; }
 .missions-head .t .ic{ inline-size:16px; block-size:16px; color:var(--warning); }
 .x-btn{ border:0; background:transparent; color:var(--muted); cursor:pointer; inline-size:32px; block-size:32px; border-radius:8px; display:grid; place-items:center; margin-inline-end:-4px; }
-.x-btn:hover{ background:var(--border); color:var(--text); }
+@media (hover:hover){ .x-btn:hover{ background:var(--border); color:var(--text); } }
 .m-chips{ display:flex; flex-direction:column; gap:7px; }
 .m-chip{ display:flex; align-items:center; gap:10px; font-size:13px; font-weight:500; color:var(--text); background:var(--surface); border:1px solid var(--border); border-radius:var(--r-ctl); padding:10px 12px; cursor:pointer; text-decoration:none; transition:border-color .15s,transform .12s; }
-.m-chip:hover{ border-color:var(--primary); transform:translateX(2px); }
-[dir="rtl"] .m-chip:hover{ transform:translateX(-2px); }
+@media (hover:hover){ .m-chip:hover{ border-color:var(--primary); transform:translateX(2px); } }
+@media (hover:hover){ [dir="rtl"] .m-chip:hover{ transform:translateX(-2px); } }
 .m-chip .mi{ inline-size:30px; block-size:30px; border-radius:9px; display:grid; place-items:center; background:var(--brand-tint); color:var(--brand-tint-ink); flex:none; }
 .m-chip .mi .ic{ inline-size:16px; block-size:16px; }
 .m-chip .mt{ flex:1; min-inline-size:0; display:flex; flex-direction:column; gap:1px; }
@@ -381,17 +381,18 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 /* HOW IT WORKS */
   .steps{ display:grid; gap:16px; grid-template-columns:1fr; }
 @media (min-width:760px){ .steps{ grid-template-columns:repeat(3,1fr); gap:20px; } }
-.step{ position:relative; background:var(--surface); border:1px solid var(--border); border-radius:var(--r-lg); padding:24px; box-shadow:var(--shadow-sm); }
-.step .num{ inline-size:40px; block-size:40px; border-radius:12px; background:var(--brand-tint); color:var(--brand-tint-ink); display:grid; place-items:center; font-size:18px; font-weight:800; margin-block-end:16px; }
+.step{ position:relative; background:var(--surface); border:1px solid var(--border); border-radius:var(--r-lg); padding:24px; box-shadow:var(--shadow-sm);
+  display:grid; grid-template-columns:auto 1fr; grid-template-areas:"icon title" "body body"; column-gap:14px; align-items:center; }
+.step .num{ grid-area:icon; inline-size:40px; block-size:40px; border-radius:12px; background:var(--brand-tint); color:var(--brand-tint-ink); display:grid; place-items:center; font-size:18px; font-weight:800; }
 .step .num .ic{ inline-size:20px; block-size:20px; }
-.step h3{ font-size:18px; font-weight:700; margin:0 0 8px; }
-.step p{ font-size:14px; color:var(--muted); margin:0; line-height:1.7; }
+.step h3{ grid-area:title; font-size:18px; font-weight:700; margin:0; }
+.step p{ grid-area:body; font-size:14px; color:var(--muted); margin-block-start:12px; line-height:1.7; }
 .step .conn{ display:none; }
 /* LOCATIONS */
   .locrow{ display:grid; grid-template-columns:repeat(2,1fr); gap:12px; }
 @media (min-width:640px){ .locrow{ grid-template-columns:repeat(4,1fr); } }
 .locbig{ display:flex; flex-direction:column; align-items:center; gap:10px; padding:22px 12px; background:var(--surface); border:1px solid var(--border); border-radius:var(--r-lg); text-decoration:none; color:var(--text); box-shadow:var(--shadow-sm); transition:transform .14s,box-shadow .14s,border-color .14s; }
-.locbig:hover{ transform:translateY(-3px); box-shadow:var(--shadow-md); border-color:color-mix(in srgb,var(--primary) 40%,var(--border)); }
+@media (hover:hover){ .locbig:hover{ transform:translateY(-3px); box-shadow:var(--shadow-md); border-color:color-mix(in srgb,var(--primary) 40%,var(--border)); } }
 .locbig .flag{ inline-size:52px; block-size:52px; }
 .locbig .nm{ font-size:15px; font-weight:700; }
 .locbig .go{ font-size:12.5px; font-weight:700; color:var(--link); display:inline-flex; align-items:center; gap:5px; }
@@ -401,7 +402,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
   .mvlist{ display:grid; gap:11px; grid-template-columns:1fr; }
 @media (min-width:760px){ .mvlist{ grid-template-columns:repeat(2,1fr); } }
 .mvrow{ display:flex; align-items:center; gap:14px; background:var(--surface); border:1px solid var(--border); border-radius:var(--r-lg); padding:16px; box-shadow:var(--shadow-sm); transition:transform .14s,box-shadow .14s,border-color .14s; }
-.mvrow:hover{ transform:translateY(-2px); box-shadow:var(--shadow-md); border-color:color-mix(in srgb,var(--primary) 35%,var(--border)); }
+@media (hover:hover){ .mvrow:hover{ transform:translateY(-2px); box-shadow:var(--shadow-md); border-color:color-mix(in srgb,var(--primary) 35%,var(--border)); } }
 .mvrow .mi{ inline-size:44px; block-size:44px; border-radius:13px; background:var(--brand-tint); color:var(--brand-tint-ink); display:grid; place-items:center; flex:none; }
 .mvrow .mi .ic{ inline-size:22px; block-size:22px; }
 .mvbd{ flex:1; min-inline-size:0; }
@@ -413,7 +414,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 /* APPS row */
   .approw{ display:grid; gap:14px; grid-template-columns:1fr; }
 .appcard{ display:flex; align-items:center; gap:14px; background:var(--surface); border:1px solid var(--border); border-radius:var(--r-lg); padding:18px; box-shadow:var(--shadow-sm); text-decoration:none; color:var(--text); transition:transform .14s,box-shadow .14s,border-color .14s; }
-.appcard:hover{ transform:translateY(-2px); box-shadow:var(--shadow-md); border-color:color-mix(in srgb,var(--primary) 40%,var(--border)); }
+@media (hover:hover){ .appcard:hover{ transform:translateY(-2px); box-shadow:var(--shadow-md); border-color:color-mix(in srgb,var(--primary) 40%,var(--border)); } }
 .appcard .big-ico{ inline-size:52px; block-size:52px; border-radius:14px; flex:none; object-fit:cover; box-shadow:inset 0 0 0 1px var(--flag-ring),var(--shadow-sm); }
 .appcard .at{ flex:1; min-inline-size:0; display:flex; flex-direction:column; align-items:flex-start; gap:3px; }
 .appcard .an{ display:inline-flex; align-items:center; gap:8px; font-size:16px; font-weight:700; }
@@ -449,6 +450,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
   .trust-card{ background:var(--surface); border:1px solid var(--border); border-radius:var(--r-hero); padding:36px 24px; text-align:center; box-shadow:var(--shadow-sm); position:relative; overflow:hidden; }
 .trust-card::before{ content:""; position:absolute; inset:0; background:radial-gradient(500px 220px at 50% -20%,var(--brand-tint),transparent 70%); }
 .trust-badges{ position:relative; display:flex; flex-wrap:wrap; justify-content:center; gap:10px; margin:20px 0; }
+@media (max-width:479px){ .trust-badges{ flex-direction:column; align-items:center; } }
 .tb{ display:inline-flex; align-items:center; gap:8px; font-size:14px; font-weight:700; background:var(--sunken); border:1px solid var(--border); border-radius:var(--r-pill); padding:9px 16px; }
 .tb .ic{ inline-size:17px; block-size:17px; color:var(--success); }
 .trust-shield{ position:relative; inline-size:60px; block-size:60px; border-radius:18px; background:var(--brand-tint); color:var(--brand-tint-ink); display:grid; place-items:center; margin:0 auto 18px; }
@@ -457,7 +459,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
   .bloggrid{ display:grid; gap:18px; grid-template-columns:1fr; }
 @media (min-width:720px){ .bloggrid{ grid-template-columns:repeat(3,1fr); } }
 .post{ background:var(--surface); border:1px solid var(--border); border-radius:var(--r-lg); overflow:hidden; box-shadow:var(--shadow-sm); text-decoration:none; color:var(--text); transition:transform .14s,box-shadow .14s; }
-.post:hover{ transform:translateY(-3px); box-shadow:var(--shadow-md); }
+@media (hover:hover){ .post:hover{ transform:translateY(-3px); box-shadow:var(--shadow-md); } }
 .post .cover{ block-size:140px; background:linear-gradient(135deg,var(--hero-1),var(--hero-2)); position:relative; }
 [dir="rtl"] .post .cover{ background:linear-gradient(225deg,var(--hero-1),var(--hero-2)); }
 .post .cover .tag{ position:absolute; inset-block-end:10px; inset-inline-start:12px; font-size:11px; font-weight:700; color:#fff; background:rgba(0,0,0,.28); border-radius:var(--r-pill); padding:4px 10px; backdrop-filter:blur(4px); }
@@ -472,7 +474,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 .ft-cta-t h3{ font-size:20px; font-weight:800; margin:0 0 6px; }
 .ft-cta-t p{ font-size:13.5px; color:var(--muted); margin:0; }
 .ft-cta-btn{ display:inline-flex; align-items:center; gap:8px; background:var(--primary); color:var(--on-primary); font-weight:800; font-size:14.5px; padding:12px 20px; border-radius:var(--r-btn); text-decoration:none; white-space:nowrap; box-shadow:0 8px 22px -10px color-mix(in srgb,var(--primary) 60%,transparent); transition:transform .1s,filter .18s; }
-.ft-cta-btn:hover{ filter:brightness(1.05); }
+@media (hover:hover){ .ft-cta-btn:hover{ filter:brightness(1.05); } }
 .ft-cta-btn:active{ transform:scale(.985); }
 .ft-cta-btn .ic{ inline-size:17px; block-size:17px; flex:none; }
 .ft-grid{ display:grid; gap:32px; grid-template-columns:1fr 1fr; padding-block:0 30px; }
@@ -481,12 +483,14 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
 .ft-brand p{ font-size:13.5px; color:var(--muted); max-width:34ch; line-height:1.85; margin:0; }
 .ft-col h4{ font-size:13.5px; font-weight:800; margin:0 0 15px; color:var(--text); }
 .ft-col a{ display:block; font-size:13.5px; color:var(--muted); text-decoration:none; padding-block:6.5px; transition:color .15s; }
-.ft-col a:hover{ color:var(--link); }
+@media (hover:hover){ .ft-col a:hover{ color:var(--link); } }
 /* Article-landing link row (above the bottom bar) — wraps freely, so it never disturbs .ft-grid. */
-.ft-more{ display:flex; flex-wrap:wrap; align-items:center; gap:8px 16px; border-block-start:1px solid var(--border); padding-block:16px; font-size:12.5px; }
-.ft-more-h{ font-weight:800; color:var(--text); }
-.ft-more a{ color:var(--muted); text-decoration:none; transition:color .15s; }
-.ft-more a:hover{ color:var(--link); }
+.ft-more{ display:flex; flex-wrap:wrap; align-items:center; gap:2px 10px; border-block-start:1px solid var(--border); padding-block:16px; font-size:12.5px; }
+.ft-more-h{ flex-basis:100%; font-weight:800; color:var(--text); margin-block-end:6px; }
+.ft-more a{ display:inline-flex; align-items:center; min-block-size:24px; padding-block:1px; color:var(--muted); text-decoration:none; transition:color .15s; }
+/* a visible separator: adjacent links with nothing between them read as one continuous phrase */
+.ft-more a + a::before{ content:""; inline-size:3px; block-size:3px; border-radius:50%; background:var(--border-strong); margin-inline-end:10px; flex:none; }
+@media (hover:hover){ .ft-more a:hover{ color:var(--link); } }
 .ft-bottom{ border-block-start:1px solid var(--border); padding-block:20px; display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap; font-size:12.5px; color:var(--muted); }
 .ft-langs{ display:inline-flex; padding:3px; gap:2px; background:var(--sunken); border:1px solid var(--border); border-radius:var(--r-pill); }
 .ft-langs button{ border:0; background:transparent; color:var(--muted); border-radius:var(--r-pill); padding:6px 15px; font:inherit; font-size:12.5px; font-weight:600; cursor:pointer; }
@@ -519,7 +523,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
      descenders (ن ی ج) never clip under overflow:hidden. */
   #app[data-locale="fa"] .loc-card .nm{ font-size:13px; font-weight:500; line-height:1.75; }
 /* ---------- toolbar ---------- */
-  .toolbar{ position:sticky; inset-block-start:0; z-index:50; background:color-mix(in srgb,var(--bg) 82%,transparent); backdrop-filter:blur(18px) saturate(1.4); border-block-end:1px solid var(--border); }
+  .toolbar{ position:sticky; inset-block-start:0; z-index:50; background:color-mix(in srgb,var(--bg) 94%,transparent); backdrop-filter:blur(18px) saturate(1.4); border-block-end:1px solid var(--border); }
 .toolbar-row{ display:flex; align-items:center; gap:12px; padding-block:12px; flex-wrap:wrap; }
 .toolbar-spacer{ flex:1; }
 /* ---------- type ---------- */
@@ -534,7 +538,7 @@ nav.mainnav{ display:none; align-items:center; gap:4px; margin-inline-start:14px
     color:#fff; background:linear-gradient(135deg,var(--hero-1),color-mix(in srgb,var(--hero-1) 55%,var(--hero-2)));
     box-shadow:var(--glow); position:relative; overflow:hidden; }
 .cta::after{ content:""; position:absolute; inset:0; background:linear-gradient(100deg,transparent 30%,rgba(255,255,255,.18) 50%,transparent 70%); transform:translateX(-120%); }
-.cta:not(:disabled):hover::after{ animation:sheen 1.1s ease; }
+@media (hover:hover){ .cta:not(:disabled):hover::after{ animation:sheen 1.1s ease; } }
 @keyframes sheen{ to{ transform:translateX(120%); } }
 /* ================= HERO ================= */
   .hero{ position:relative; overflow:hidden; padding-block:28px 44px; }
@@ -785,9 +789,9 @@ footer.ft{ border-block-start:1px solid var(--border); background:var(--surface)
 .btn.grad{ color:#fff; background:linear-gradient(135deg,var(--hero-1),color-mix(in srgb,var(--hero-1) 55%,var(--hero-2))); box-shadow:var(--glow-sm); }
 [dir="rtl"] .btn.grad{ background:linear-gradient(225deg,var(--hero-1),color-mix(in srgb,var(--hero-1) 55%,var(--hero-2))); }
 #app[data-theme="dark"] .btn.grad{ color:#EAF2FF; }
-.btn.grad:hover{ filter:brightness(1.06); }
+@media (hover:hover){ .btn.grad:hover{ filter:brightness(1.06); } }
 /* header (same as homepage) */
-  header.hd{ position:sticky; inset-block-start:0; z-index:60; background:color-mix(in srgb,var(--bg) 82%,transparent); backdrop-filter:blur(18px) saturate(1.4); border-block-end:1px solid var(--border); }
+  header.hd{ position:sticky; inset-block-start:0; z-index:60; background:color-mix(in srgb,var(--bg) 94%,transparent); backdrop-filter:blur(18px) saturate(1.4); border-block-end:1px solid var(--border); }
 /* page (rewards page — scoped so it doesn't center/constrain the content-page headers) */
   .mgrid-page .page-head{ text-align:center; max-width:44rem; margin:0 auto; padding-block:36px 8px; }
 .page-head .eyebrow{ margin-block-end:14px; }
@@ -840,7 +844,7 @@ footer.ft{ border-block-start:1px solid var(--border); background:var(--surface)
 .mgrid-page .sec-title{ font-size:clamp(22px,3.6vw,28px); margin:0 0 6px; }
 .sec-lede{ font-size:15px; color:var(--muted); margin:0 0 24px; }
 .mgrid{ display:grid; gap:16px; grid-template-columns:1fr; }
-.mcard[data-state="available"]:hover{ transform:translateY(-2px); box-shadow:var(--shadow-md); border-color:color-mix(in srgb,var(--primary) 35%,var(--border)); }
+@media (hover:hover){ .mcard[data-state="available"]:hover{ transform:translateY(-2px); box-shadow:var(--shadow-md); border-color:color-mix(in srgb,var(--primary) 35%,var(--border)); } }
 .mcard[data-state="done"]{ border-color:color-mix(in srgb,var(--success) 40%,var(--border)); background:linear-gradient(180deg,var(--success-surface),transparent 60%); }
 .mcard[data-state="unavailable"]{ opacity:.72; }
 .mcard .top{ display:flex; align-items:flex-start; justify-content:space-between; gap:12px; margin-block-end:14px; }
@@ -856,7 +860,8 @@ footer.ft{ border-block-start:1px solid var(--border); background:var(--surface)
 .why .qm{ inline-size:17px; block-size:17px; border-radius:50%; border:1px solid var(--border-strong); color:var(--muted); font-size:11px; font-weight:800; display:grid; place-items:center; cursor:help; }
 [dir="rtl"] .mcard .why{ margin-inline-start:0; margin-inline-end:6px; }
 .why .tip{ position:absolute; inset-block-end:calc(100% + 8px); inset-inline-end:-6px; inline-size:200px; background:var(--band); color:var(--band-ink); font-size:11.5px; font-weight:500; line-height:1.6; padding:9px 11px; border-radius:10px; box-shadow:var(--shadow-md); opacity:0; pointer-events:none; transition:opacity .15s; z-index:5; }
-.why:hover .tip, .why:focus-within .tip{ opacity:1; }
+.why:focus-within .tip{ opacity:1; }
+@media (hover:hover){ @media (hover:hover){ .why:hover .tip{ opacity:1; } } }
 /* streak dots */
   .streak{ display:flex; justify-content:center; gap:8px; margin-block-start:10px; }
 .streak .d{ flex:1 1 0; max-inline-size:44px; aspect-ratio:1; border-radius:11px; background:var(--sunken-2); display:grid; place-items:center; font-size:12.5px; font-weight:700; color:var(--muted); }
@@ -888,9 +893,9 @@ footer.ft{ border-block-start:1px solid var(--border); background:var(--surface)
 .grid-2 { display: grid; gap: 16px; }
 @media (min-width: 720px) { .grid-2 { grid-template-columns: 1fr 1fr; } }
 .btn-primary { background: var(--primary); color: var(--on-primary); box-shadow: var(--glow-sm); border-radius: var(--r-btn); padding: 12px 20px; font-weight: 700; display: inline-flex; align-items: center; justify-content: center; gap: 8px; }
-.btn-primary:hover { background: var(--primary-hover); }
+@media (hover:hover){ .btn-primary:hover { background: var(--primary-hover); } }
 .btn-ghost { background: var(--sunken); color: var(--text); border: 1px solid var(--border); border-radius: var(--r-btn); padding: 12px 20px; font-weight: 700; display: inline-flex; align-items: center; justify-content: center; gap: 8px; }
-.btn-ghost:hover { border-color: var(--border-strong); }
+@media (hover:hover){ .btn-ghost:hover { border-color: var(--border-strong); } }
 .btn-lg { padding: 15px 26px; font-size: 17px; }
 .btn-block { width: 100%; }
 .codebox { direction: ltr; text-align: left; unicode-bidi: isolate; font-family: ui-monospace, "SF Mono", Menlo, monospace; background: var(--sunken); border: 1px solid var(--border); border-radius: var(--r-ctl); padding: 12px 14px; font-size: 13px; word-break: break-all; color: var(--text); }
@@ -1001,7 +1006,7 @@ select.inp{ appearance:none; background-image:linear-gradient(45deg,transparent
 .id-bar .id-icb .ic{ inline-size:18px; block-size:18px; }
 .id-bar .id-code{ font-family:ui-monospace,"SF Mono",Consolas,monospace; font-weight:800; letter-spacing:.6px; font-size:15px; color:var(--text); min-inline-size:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
 .id-bar .id-cp{ inline-size:32px; block-size:32px; border-radius:9px; border:1px solid var(--border); background:var(--sunken); display:grid; place-items:center; cursor:pointer; color:var(--muted); flex:none; transition:color .15s,border-color .15s; }
-.id-bar .id-cp:hover{ color:var(--primary); border-color:var(--primary); }
+@media (hover:hover){ .id-bar .id-cp:hover{ color:var(--primary); border-color:var(--primary); } }
 .id-bar .id-cp.done{ color:var(--success-ink); border-color:color-mix(in srgb,var(--success) 40%,var(--border)); }
 .id-bar .id-cp .ic{ inline-size:15px; block-size:15px; }
 .id-bar .id-sep{ inline-size:1px; align-self:stretch; background:var(--border); margin-inline:2px; flex:none; }
@@ -1069,7 +1074,7 @@ select.inp{ appearance:none; background-image:linear-gradient(45deg,transparent
 .rw2-list{ display:flex; flex-direction:column; }
 .rw2-row{ display:flex; align-items:center; gap:13px; inline-size:100%; padding:14px 2px; text-align:start; background:none; border:0; font:inherit; color:inherit; }
 button.rw2-row{ cursor:pointer; -webkit-tap-highlight-color:transparent; border-radius:12px; transition:background .14s; }
-button.rw2-row:hover{ background:color-mix(in srgb,var(--primary) 5%,transparent); }
+@media (hover:hover){ button.rw2-row:hover{ background:color-mix(in srgb,var(--primary) 5%,transparent); } }
 button.rw2-row:disabled{ cursor:default; background:none; }
 .rw2-row + .rw2-row{ border-block-start:1px solid var(--border); }
 
@@ -1111,7 +1116,7 @@ button.rw2-row:disabled{ cursor:default; background:none; }
 .m-chip .chip-end{ flex:none; display:inline-flex; align-items:center; color:var(--muted); }
 .m-chip .chip-end .ic{ inline-size:18px; block-size:18px; }
 .m-chip:disabled{ cursor:default; opacity:1; }
-.m-chip:disabled:hover{ border-color:var(--border); transform:none; }
+@media (hover:hover){ .m-chip:disabled:hover{ border-color:var(--border); transform:none; } }
 
 /* iOS install steps modal */
 .ios-steps{ display:flex; flex-direction:column; gap:10px; }
@@ -1146,10 +1151,10 @@ button.rw2-row:disabled{ cursor:default; background:none; }
   transition:transform .16s, box-shadow .16s, border-color .16s; }
 .gcard::before{ content:""; position:absolute; inset-block:0; inset-inline-start:0; inline-size:3px;
   background:var(--acc); opacity:0; transition:opacity .16s; }
-.gcard:hover{ transform:translateY(-3px);
+@media (hover:hover){ .gcard:hover{ transform:translateY(-3px);
   border-color:color-mix(in srgb,var(--acc) 42%,var(--border));
-  box-shadow:0 14px 28px -16px color-mix(in srgb,var(--acc) 55%,transparent), var(--shadow-md); }
-.gcard:hover::before{ opacity:1; }
+  box-shadow:0 14px 28px -16px color-mix(in srgb,var(--acc) 55%,transparent), var(--shadow-md); } }
+@media (hover:hover){ .gcard:hover::before{ opacity:1; } }
 .gos{ flex:0 0 auto; inline-size:50px; block-size:50px; border-radius:15px; display:grid; place-items:center;
   position:relative; color:var(--acc);
   background:color-mix(in srgb,var(--acc) 14%,var(--surface));
@@ -1170,7 +1175,7 @@ button.rw2-row:disabled{ cursor:default; background:none; }
 .gtime .ic{ inline-size:14px; block-size:14px; }
 .gchevw{ flex:0 0 auto; inline-size:34px; block-size:34px; border-radius:50%; display:grid; place-items:center;
   background:var(--sunken); color:var(--muted); transition:background .16s, color .16s; }
-.gcard:hover .gchevw{ background:color-mix(in srgb,var(--acc) 14%,var(--surface)); color:var(--acc); }
+@media (hover:hover){ .gcard:hover .gchevw{ background:color-mix(in srgb,var(--acc) 14%,var(--surface)); color:var(--acc); } }
 [dir="ltr"] .gchev{ transform:scaleX(-1); }  /* drill-in points to the reading-forward edge */
 
 .guide-page{ padding-block:28px 56px; }
@@ -1202,8 +1207,8 @@ button.rw2-row:disabled{ cursor:default; background:none; }
   background:var(--text); color:var(--surface); border-radius:var(--r-btn); padding:9px 16px 9px 13px;
   margin-block-start:15px; box-shadow:0 10px 22px -12px color-mix(in srgb,var(--text) 55%,transparent);
   transition:transform .12s, filter .15s, box-shadow .15s; }
-.dlbtn:hover{ transform:translateY(-2px); filter:brightness(1.06);
-  box-shadow:0 14px 26px -12px color-mix(in srgb,var(--acc) 60%,transparent); }
+@media (hover:hover){ .dlbtn:hover{ transform:translateY(-2px); filter:brightness(1.06);
+  box-shadow:0 14px 26px -12px color-mix(in srgb,var(--acc) 60%,transparent); } }
 .dlbtn .store{ flex:0 0 auto; display:grid; place-items:center; }
 .dlbtn .store svg{ inline-size:24px; block-size:24px; }
 .dlbtn .dlt{ direction:rtl; display:flex; flex-direction:column; text-align:right; line-height:1.18; }
@@ -1236,7 +1241,7 @@ button.rw2-row:disabled{ cursor:default; background:none; }
 .locdiv{ block-size:1px; margin:18px 24px 0; background:linear-gradient(90deg, transparent, var(--border) 20%, var(--border) 80%, transparent); }
 /* 5 flags + a "+N" circle = 6 items on one row down to ~360px; flex:0 0 auto so a circle never
    shrinks into an oval, wrap only as a last resort on very narrow screens. */
-.flagstrip{ display:flex; align-items:center; justify-content:center; flex-wrap:wrap; gap:12px; padding:18px 14px 4px; }
+.flagstrip{ display:flex; align-items:center; justify-content:center; flex-wrap:wrap; gap:12px; padding:18px 6px 4px; }
 .flagstrip .fbig, .flagmore{ flex:0 0 auto; inline-size:44px; block-size:44px; border-radius:50%; }
 .flagstrip .fbig{ object-fit:cover; box-shadow:0 3px 10px -2px color-mix(in srgb,var(--text) 20%,transparent), 0 0 0 3px var(--surface); }
 .fbig.fb-fallback{ display:grid; place-items:center; background:var(--sunken); color:var(--muted); font-weight:800; font-size:13px; }
@@ -1279,7 +1284,7 @@ button.rw2-row:disabled{ cursor:default; background:none; }
 /* ===== SEO landing pages (/l/[slug]) + locations index (/locations) ===== */
 .landing-page .crumbs{ display:flex; align-items:center; gap:8px; font-size:13px; color:var(--muted); margin-block-end:14px; }
 .landing-page .crumbs a{ color:var(--muted); text-decoration:none; }
-.landing-page .crumbs a:hover{ color:var(--text); }
+@media (hover:hover){ .landing-page .crumbs a:hover{ color:var(--text); } }
 .landing-page h1{ font-size:clamp(26px,4.2vw,34px); line-height:1.35; margin-block-end:14px; }
 /* admin-authored article body: comfortable fa reading rhythm (spec: ~38rem measure, 1.8 leading) */
 .landing-body{ max-inline-size:38rem; line-height:1.9; font-size:15.5px; color:var(--text); }
@@ -1287,24 +1292,24 @@ button.rw2-row:disabled{ cursor:default; background:none; }
 .landing-body h2{ font-size:19px; margin-block:26px 8px; }
 .landing-body ul{ padding-inline-start:22px; margin-block:10px 14px; display:grid; gap:6px; }
 .landing-body a{ color:var(--primary); text-decoration:none; font-weight:600; }
-.landing-body a:hover{ text-decoration:underline; }
+@media (hover:hover){ .landing-body a:hover{ text-decoration:underline; } }
 .landing-body code{ direction:ltr; unicode-bidi:isolate; background:var(--sunken); border:1px solid var(--border); border-radius:6px; padding:1px 6px; font-size:13px; }
 .landing-widget{ margin-block:28px; }
 /* Homepage articles band — link chips on .chip's base look (centered under the section head). */
 .art-chips{ display:flex; flex-wrap:wrap; gap:10px; justify-content:center; }
 .art-chips .chip{ text-decoration:none; padding:8px 15px; font-size:13px; transition:color .15s,border-color .15s; }
-.art-chips .chip:hover{ border-color:var(--border-strong); color:var(--text); }
+@media (hover:hover){ .art-chips .chip:hover{ border-color:var(--border-strong); color:var(--text); } }
 .landing-related h2{ font-size:17px; margin-block-end:10px; }
 .landing-related .chips{ display:flex; flex-wrap:wrap; gap:8px; }
 .landing-related .chip{ text-decoration:none; padding:7px 14px; font-size:13px; }
-.landing-related .chip:hover{ border-color:var(--border-strong); color:var(--text); }
+@media (hover:hover){ .landing-related .chip:hover{ border-color:var(--border-strong); color:var(--text); } }
 
 .locations-page .sec-head{ margin-block-end:18px; }
 .loc-intro{ max-inline-size:40rem; margin-inline:auto; line-height:1.9; color:var(--muted); text-align:center; display:grid; gap:8px; margin-block-end:30px; }
 .loc-h2{ font-size:19px; margin-block:26px 14px; }
 .loc-cards{ display:grid; grid-template-columns:repeat(auto-fill,minmax(240px,1fr)); gap:14px; }
 .loc-card-link{ display:block; text-decoration:none; color:inherit; background:var(--surface); border:1px solid var(--border); border-radius:var(--r-lg); padding:16px; transition:border-color .15s ease, transform .15s ease; }
-.loc-card-link:hover{ border-color:var(--border-strong); transform:translateY(-2px); }
+@media (hover:hover){ .loc-card-link:hover{ border-color:var(--border-strong); transform:translateY(-2px); } }
 .loc-card-link b{ display:block; font-size:15px; margin-block-end:6px; }
 .loc-card-link p{ font-size:12.5px; line-height:1.7; color:var(--muted); margin-block-end:10px;
   display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical; overflow:hidden; }
@@ -1312,7 +1317,7 @@ button.rw2-row:disabled{ cursor:default; background:none; }
 .loc-card-link .more .ic{ inline-size:14px; block-size:14px; }
 .locgrid{ display:grid; grid-template-columns:repeat(auto-fill,minmax(150px,1fr)); gap:10px; }
 .loccell{ display:flex; align-items:center; gap:10px; text-decoration:none; color:inherit; background:var(--surface); border:1px solid var(--border); border-radius:var(--r-lg); padding:10px 12px; min-block-size:56px; transition:border-color .15s ease; }
-.loccell:hover{ border-color:var(--border-strong); }
+@media (hover:hover){ .loccell:hover{ border-color:var(--border-strong); } }
 .loccell .ln{ font-size:13.5px; font-weight:700; flex:1; min-inline-size:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
 .loccell.skeleton{ border-style:dashed; }
 
@@ -1337,20 +1342,20 @@ button.rw2-row:disabled{ cursor:default; background:none; }
 .ts-retry{ flex:none; font:inherit; font-weight:700; font-size:12.5px; padding:5px 12px;
   border-radius:9px; cursor:pointer; color:var(--danger-ink); background:transparent;
   border:1px solid color-mix(in srgb,var(--danger) 45%,transparent); }
-.ts-retry:hover{ background:color-mix(in srgb,var(--danger) 12%,transparent); }
+@media (hover:hover){ .ts-retry:hover{ background:color-mix(in srgb,var(--danger) 12%,transparent); } }
 
 /* change-location: an inviting list row — rotating swap chip + label + real alternatives count */
 .chg-btn{ display:flex; align-items:center; gap:12px; inline-size:100%; margin-block-start:16px; padding:9px 12px; background:transparent; border:1px solid var(--border); border-radius:var(--r-ctl); cursor:pointer; font:inherit; color:var(--text); text-align:start; transition:border-color .15s, background .15s; }
-.chg-btn:hover{ border-color:var(--primary); background:var(--brand-tint); }
+@media (hover:hover){ .chg-btn:hover{ border-color:var(--primary); background:var(--brand-tint); } }
 .chg-btn:focus-visible{ outline:2px solid var(--ring); outline-offset:2px; }
 .chg-btn .ci{ inline-size:36px; block-size:36px; border-radius:12px; flex:none; display:grid; place-items:center; background:var(--brand-tint); color:var(--brand-tint-ink); transition:transform .35s ease; }
 .chg-btn .ci .ic{ inline-size:18px; block-size:18px; }
-.chg-btn:hover .ci{ transform:rotate(180deg); }
+@media (hover:hover){ .chg-btn:hover .ci{ transform:rotate(180deg); } }
 .chg-btn .ct{ flex:1; min-inline-size:0; display:grid; gap:1px; }
 .chg-btn .ct b{ font-size:13.5px; font-weight:700; }
 .chg-btn .ct small{ font-size:11.5px; color:var(--muted); }
 .chg-btn .chg-chev{ inline-size:15px; block-size:15px; color:var(--muted); opacity:.75; flex:none; transition:color .15s, opacity .15s; }
-.chg-btn:hover .chg-chev{ color:var(--primary); opacity:1; }
+@media (hover:hover){ .chg-btn:hover .chg-chev{ color:var(--primary); opacity:1; } }
 [dir="rtl"] .chg-btn .chg-chev{ transform:scaleX(-1); }
 
 /* header account button — brand-tint chip + idcard icon (the account cards' tile language).
@@ -1358,15 +1363,15 @@ button.rw2-row:disabled{ cursor:default; background:none; }
    the stale .status-btn{display:none} win and the chip vanished from phones). */
 .acct-btn.status-btn{ display:inline-flex; }
 .acct-btn{ align-items:center; gap:8px; font-size:13.5px; font-weight:700; padding:9px 15px; border-radius:12px; background:var(--brand-tint); color:var(--brand-tint-ink); text-decoration:none; border:1px solid transparent; transition:background .15s; }
-.acct-btn:hover{ background:var(--brand-tint-2); }
+@media (hover:hover){ .acct-btn:hover{ background:var(--brand-tint-2); } }
 .acct-btn:focus-visible{ outline:2px solid var(--ring); outline-offset:2px; }
 .acct-btn .ic{ inline-size:16px; block-size:16px; flex:none; }
 
 /* mobile: the account chip collapses to an icon-only square with the burger's EXACT footprint
    (38x38 r11; 36x36 on <=360px like .icon-only) — the person icon alone, no label, tint kept */
 @media (max-width:999.98px){
-  .acct-btn{ inline-size:38px; block-size:38px; padding:0; border-radius:11px; justify-content:center; }
+  .acct-btn{ inline-size:44px; block-size:44px; padding:0; border-radius:13px; justify-content:center; }
   .acct-btn .acct-lbl{ display:none; }
   .acct-btn .ic{ inline-size:19px; block-size:19px; }
 }
-@media (max-width:360px){ .acct-btn{ inline-size:36px; block-size:36px; } }
+
diff --git a/frontend/site/app/page.tsx b/frontend/site/app/page.tsx
index 8f8f46e..6ad2f33 100644
--- a/frontend/site/app/page.tsx
+++ b/frontend/site/app/page.tsx
@@ -3,7 +3,6 @@ import Link from "next/link";
 import { getLocale } from "@/lib/server";
 import { translator } from "@/lib/i18n";
 import { fetchSiteCopy } from "@/lib/siteCopy";
-import { fetchArticleLandings } from "@/lib/landing";
 
 // Self-referencing canonical for the homepage. Set here (not in the root layout) so it applies only
 // to `/` — a layout-level canonical would be inherited by every sub-page and wrongly mark them all
@@ -30,12 +29,6 @@ export default async function HomePage() {
   // The translator takes the panel's overrides as its top layer, so every allowlisted design-copy
   // key on this page is editable without a redeploy. With none set it behaves exactly as before.
   const t = translator(locale, copy.overrides);
-  // Article/guide landings, linked from the homepage so they inherit real internal links instead of
-  // being sitemap-only orphans (see fetchArticleLandings). Empty list ⇒ the band renders nothing.
-  // The cap is deliberately above the seeded count: a low cap silently dropped the tail of the slug
-  // ordering (v2rayng-config landed there), leaving exactly the pages this band exists to link.
-  const articles = await fetchArticleLandings(24);
-
   const steps = [
     { t: "how1_t", d: "how1_d", ic: "pin" },
     { t: "how2_t", d: "how2_d", ic: "bolt" },
@@ -120,27 +113,9 @@ export default async function HomePage() {
       {/* FAQ teaser (live accordion) */}
       <HomeFaq locale={locale} copy={copy.overrides} />
 
-      {/* ARTICLES & GUIDES — server-rendered internal links to the /l/* article landings, so the
-          anchors are in the raw HTML a crawler sees (no JS) with the page's own keyword as text. */}
-      {articles.length > 0 && (
-        <section className="sec" id="articles">
-          <div className="container">
-            <div className="sec-head reveal">
-              <span className="eyebrow">{t("art_eyebrow")}</span>
-              <h2 className="sec-title">{t("art_title")}</h2>
-              <p className="sec-sub">{t("art_sub")}</p>
-            </div>
-            <div className="art-chips reveal">
-              {articles.map((a) => (
-                <Link key={a.slug} className="chip" href={`/l/${a.slug}`}>
-                  {/* bdi: fa labels stay readable inside en (LTR) chrome — same as landing chips */}
-                  <bdi>{a.label}</bdi>
-                </Link>
-              ))}
-            </div>
-          </div>
-        </section>
-      )}
+      {/* ARTICLES & GUIDES — the same 13 links used to render here AND in the footer, on every
+          page. Two instances of one list split the internal-link equity for no gain and cost 900px
+          of homepage; the footer row (components/Footer.tsx) is now the single instance. */}
 
       {/* TRUST band */}
       <section className="sec">
